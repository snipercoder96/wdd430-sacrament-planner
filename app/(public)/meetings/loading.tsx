export default function Loading() {
    return (
        <main className="meetings-content" aria-busy="true">
            <div className="meetings-heading">
                <p className="section-eyebrow">Sacrament planner</p>
                <h1>Loading meetings...</h1>
            </div>
        </main>
    );
}